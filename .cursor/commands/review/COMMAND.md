Review the current code changes against `react-native/RN_PROJECT_CONTEXT.md`.

1. Run `git diff` (or `git diff --staged` if staged)
2. Use the rn-code-reviewer subagent to analyze modified files
3. Output: Critical / Warnings / Suggestions with concrete fix examples
